    // src/app/models/jwt-response.model.ts

    /**
     * Interface representing the structure of the JWT authentication response.
     * This matches the JwtResponse class on your Spring Boot backend.
     */
    export interface JwtResponse {
      jwtToken: string;
    }
    