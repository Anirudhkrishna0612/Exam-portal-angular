// src/app/pages/login/login-data.ts

// This interface defines the structure for the data sent during a login request.
// It acts as a Data Transfer Object (DTO) for the frontend.
export interface LoginData {
    username: string;
    password: string;
}
